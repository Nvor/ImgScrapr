import React from 'react'

const Header = ({ title }) => {
    return (
        <header>
            <h1>{title}</h1>
        </header>
    )
}

// const headerStyle = {
//     color: 'black'
// }

export default Header